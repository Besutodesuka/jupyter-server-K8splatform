apiVersion: v1
kind: Service
metadata:
  name: provisioner
  namespace: jupyter-platform
spec:
  type: NodePort
  selector:
    app: provisioner
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
