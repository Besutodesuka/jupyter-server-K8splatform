apiVersion: v1
kind: Service
metadata:
  name: jupyter-service-{{ .Values.studentId }}
  namespace: student-{{ .Values.studentId }}
spec:
  selector:
    student: {{ .Values.studentId | quote }}
  ports:
    - port: 8888
      targetPort: 8888
  type: ClusterIP
